# Microservice stack defaults
REGISTRY_HOST=0.0.0.0
REGISTRY_PORT=9090
GATEWAY_HOST=0.0.0.0
GATEWAY_PORT=9008

USER_SERVICE_NAME=user-service
USER_SERVICE_PORT=9101
USER_SERVICE_ROUTE=/api/users

ORDER_SERVICE_NAME=order-service
ORDER_SERVICE_PORT=9102
ORDER_SERVICE_ROUTE=/api/orders

# Optional hard fallback for unresolved gateway services
# GATEWAY_FALLBACK_TARGET=http://127.0.0.1:9101

# Optional explicit extension paths.
# If omitted and file exists, scripts auto-detect local build outputs.
# CORE_SO=
# DISCOVERY_SO=
# GATEWAY_SO=
